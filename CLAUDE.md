# claudecode

投資/マネー特化のnoteアカウント向けに、「会社」のような部門構成で調査・執筆をClaude Codeに行わせるプロジェクト。

## 構成
- `departments/` : 各部門。部門ごとに `instructions.md`(職務規定)と成果物を持つ
  - `research/investing` : 投資入門シリーズの次回テーマについて、制度や証券会社の最新情報を調査(`memos/`)
  - `blog/series-investing-start` : 投資入門シリーズの執筆。ネタの大枠と実行済み記録は`roadmap.md`で管理し、基本は前回記事の「次回予告」から次のお題を拾う(`posts/`)
  - `editorial` : 投資入門シリーズの記事を校閲(最大3往復)。トーン・読みやすさに加え、売買推奨表現や免責事項の有無などコンプライアンスも確認
- `management/logs/` : 各部門の実行ログ
- `management/routines.md` : 自動実行ルーティンの設定内容・変更履歴(claude.ai側の設定はここに無いので記録用)

## 基本方針
- 単一ジャンル(投資/マネー)に絞ったnoteアカウント。まずは「投資入門シリーズ」(始め方編→用語解説編)を歯抜けなく1本の流れで進める。時事ネタなど他のコンテンツ形式は、このシリーズが一巡してから改めて検討する
- **個別の金融商品・銘柄の売買は推奨しない。** 情報提供・教育に徹し、本文末尾に免責事項を必ず入れる(詳細は`departments/blog/series-investing-start/instructions.md`参照)
- 執筆フローは「roadmap確認 → リサーチ部門 → 執筆部門 → 校閲部門 → roadmap更新」の1話分を1サイクルとして自動ルーティンで回す想定(トピックの選定も含めてユーザーへの都度確認は不要とする)
- **noteへの実際の投稿(公開)は自動化せず、ユーザーが手動で行う。** 非公式APIやブラウザ自動操作による自動投稿は、noteに公式APIが無く規約違反・アカウント凍結のリスクがあるため、現時点では採用しない。下書きが数日分溜まってもよく、まとめて確認・投稿する運用でよい
- **投稿前の最終調整は、noteのエディタ上ではなくリポジトリのファイル側で行う。** 次回自動実行は記事ファイル(特に「次回予告」の文言)を見てお題を決めるため、note上でだけ文言を直すとリポジトリ側の記録とズレる。修正したくなったら、まずファイルを直し、その内容をnoteにコピペする。もしnote上で直接直してしまった場合は、忘れずに同じ修正をリポジトリのファイルにも反映すること
- アイキャッチ画像はArtifactツール(HTML/CSS)で作成し、URLをユーザーに共有する(ユーザー側でスクリーンショットしてアップロード)

## 日々のやり取りの流れ
- **「今日の記事ください」「おはよう」など、朝に記事を求められたら**: まず`git fetch`し、`origin/claude/daily-reports`を`main`にmergeしてpushする(ルーティンは`claude/daily-reports`ブランチにしかpushできないため、`main`への取り込みはこの会話の中で毎回行う)。新着が無ければ「ルーティンからの新着なし」と伝える
- 取り込んだら、その日書かれた記事(`departments/blog/series-investing-start/posts/`の最新ファイル)の中身をそのまま見せる。この時点ではアイキャッチは作らない
- ユーザーが記事の中身を確認し、修正指示があれば記事ファイルを直接編集する。問題なければ、ユーザーから「アイキャッチも作って」等の依頼を受けてから`departments/blog/series-investing-start/instructions.md`のアイキャッチ節に従って作成する(校閲・内容確定後に作る方針のため、記事の中身を見せる前に先回りして作らない)
- 投稿(noteへのアップロード)自体はユーザーが手動で行う

## 注意事項
- リポジトリはPublicにする場合、内容に機密情報が無いことを確認すること
